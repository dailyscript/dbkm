<?php
$ruta = APP_PATH . 'temp/backup/' . $backup->archivo;

header( "Content-Disposition: attachment; filename=".$backup->archivo."");
header( "Content-type: application/octet-stream" );
header("Content-Length: ".$backup->tamano); //header que envia al navegador el tamaño del archivo.
readfile($ruta);